# Regex: `\d\w\s`

**Flavor:** JavaScript

- **Sequence**
  - Matches any digit `\d` (0-9)
  - Matches any word character `\w` (a-z, A-Z, 0-9, _)
  - Matches any whitespace character `\s` (space, tab, newline, etc.)
